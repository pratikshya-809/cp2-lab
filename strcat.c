//a program to concatenate two strings strcat
#include<stdio.h>
#include<string.h>
void stringcopy (char S[])
{
	int i;
	for(i=0;S[i]!=0;i++)
	return char;
}
main()
{
	charS[]="hello";
	charS[]="world";
	strcat(S,S);
	printf("%s",s1);hello
	printf("%s",s2);world
}
